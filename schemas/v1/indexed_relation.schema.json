{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://x2knwldg.local/schemas/v1/indexed_relation.schema.json",
  "title": "IndexedRelation (v1)",
  "description": "One edge as the index and the API expose it, addressing both endpoints by three-part global id. Canvas plan §10.5. Three vocabularies coexist and must stay distinguishable: the 16 canonical relation types written by the extraction pipeline, the two synthetic edges library.py adds, and free-form user links. A user relation is never written back into relationships.json.",
  "type": "object",
  "required": [
    "schema_version",
    "id",
    "from_id",
    "to_id",
    "relation",
    "relation_vocabulary",
    "provenance_class"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "$ref": "common.schema.json#/$defs/schemaVersion" },
    "id": {
      "description": "Stable edge id. Deterministic for canonical and synthetic edges so a rebuild produces the identical set (rebuild-equivalence, T-104); workspace-generated for user edges.",
      "type": "string",
      "minLength": 1,
      "maxLength": 1300
    },
    "from_id": { "$ref": "common.schema.json#/$defs/globalId" },
    "to_id": { "$ref": "common.schema.json#/$defs/globalId" },
    "relation": { "type": "string", "minLength": 1, "maxLength": 128 },
    "relation_vocabulary": {
      "description": "'canonical' — from relationships.json, restricted to constants.RELATION_TYPES. 'library_synthetic' — derived_from and expresses_concept, generated by library.py and deliberately outside RELATION_TYPES; the Map must style them without pretending they are canonical. 'user' — a manual workspace link.",
      "enum": ["canonical", "library_synthetic", "user"]
    },
    "provenance_class": { "$ref": "common.schema.json#/$defs/provenanceClass" },
    "confidence": {
      "description": "Copied from the canonical edge. Null for user relations, which have no confidence and must never be given a fabricated one.",
      "oneOf": [
        { "$ref": "common.schema.json#/$defs/confidence" },
        { "type": "null" }
      ]
    },
    "source_id": {
      "description": "The source the edge came from. Null for a cross-source or user edge.",
      "oneOf": [
        { "$ref": "common.schema.json#/$defs/sourceId" },
        { "type": "null" }
      ]
    },
    "canonical_path": {
      "description": "Project-relative path of the file the edge was read from. Null for user relations.",
      "oneOf": [
        { "$ref": "common.schema.json#/$defs/projectRelativePath" },
        { "type": "null" }
      ]
    },
    "intentional_self_loop": {
      "description": "Mirrors the canonical flag that validators.py:117 checks; without it a self-loop is an error, not a design.",
      "type": "boolean"
    },
    "created_at": {
      "oneOf": [
        { "$ref": "common.schema.json#/$defs/isoTimestamp" },
        { "type": "null" }
      ]
    }
  },
  "allOf": [
    {
      "description": "A canonical edge uses the controlled vocabulary, carries a confidence, and points at a canonical file.",
      "if": {
        "properties": { "relation_vocabulary": { "const": "canonical" } },
        "required": ["relation_vocabulary"]
      },
      "then": {
        "required": ["relation", "confidence", "source_id"],
        "properties": {
          "relation": { "$ref": "common.schema.json#/$defs/canonicalRelationType" },
          "confidence": { "type": "number" },
          "source_id": { "type": "string" },
          "provenance_class": { "enum": ["source", "derived"] }
        }
      }
    },
    {
      "description": "The two library-synthetic edge types are derived synthesis, never source evidence.",
      "if": {
        "properties": { "relation_vocabulary": { "const": "library_synthetic" } },
        "required": ["relation_vocabulary"]
      },
      "then": {
        "properties": {
          "relation": { "$ref": "common.schema.json#/$defs/librarySyntheticRelationType" },
          "provenance_class": { "const": "derived" }
        }
      }
    },
    {
      "description": "A user relation lives only in the workspace: user provenance, no canonical file, no invented confidence.",
      "if": {
        "properties": { "relation_vocabulary": { "const": "user" } },
        "required": ["relation_vocabulary"]
      },
      "then": {
        "properties": {
          "provenance_class": { "const": "user" },
          "canonical_path": { "type": "null" },
          "confidence": { "type": "null" }
        }
      }
    }
  ]
}
